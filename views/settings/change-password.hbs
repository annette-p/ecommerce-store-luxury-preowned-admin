views/settings/change-password.hbs{{#extends 'settings'}}

    {{#block "profileContent"}}
        <div class="row mt-4">
            <div class="card mt-4 admin-regis-form" style="width: 50rem;">
                <div class="card-body">
                    <form method="POST">
                        <input type="hidden" name="_csrf" value="{{csrfToken}}"/>

                        {{{changePasswordForm}}}

                        {{!-- submit button --}}
                        <div class="row mb-4">
                            <div class="col ms-3 me-3">
                                <div class="d-grid gap-2 mt-3">
                                    <input class="btn btn-secondary" type="submit" value="Change Password"/>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    {{/block}}

{{/extends}}